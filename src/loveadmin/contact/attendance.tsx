import React, { ReactElement } from "react";

interface AttendanceProps {}

const Attendance: React.FC<AttendanceProps> = (): ReactElement => {
  return (
    <div>
      <h1>Attendance Component</h1>
    </div>
  );
};

export default Attendance;
