import React, { ReactElement } from "react";

interface MessagingProps {}

const Messaging: React.FC<MessagingProps> = (): ReactElement => {
  return (
    <div>
      <h1>Messaging Component</h1>
    </div>
  );
};

export default Messaging;
